package com.yapapp.Repository;

import com.yapapp.Model.UserToChannelModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserToChannelRepository extends JpaRepository<UserToChannelModel, Long>{

}
